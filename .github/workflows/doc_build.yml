#
# doc_build.yml
#
# Copyright The Catarina-A1 Contributors.
#
# Catarina-A1 Documentation
#
# This work is licensed under the Creative Commons Attribution-ShareAlike 4.0
# International License. To view a copy of this license,
# visit http://creativecommons.org/licenses/by-sa/4.0/.
#
#

name: Documentation Build

on:
  push:
    branches: [dev]
  pull_request:
    branches: [main, dev]

jobs:
  build_html:
    name: Build HTML Page
    runs-on: ubuntu-latest

    steps:
      - name: Set up Git repository
        uses: actions/checkout@v6

      - uses: ammaraskar/sphinx-action@master
        with:
          docs-folder: "."
          pre-build-command: "pip install -r requirements.txt --upgrade pip"

      # Create an artifact of the html output
      - uses: actions/upload-artifact@v7
        with:
          name: documentation_html
          path: _build/html/

  build_pdf:
    name: Build PDF document
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install LaTeX dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            latexmk \
            texlive-latex-recommended \
            texlive-latex-extra \
            texlive-fonts-recommended \
            texlive-lang-english \
            texlive-fonts-extra

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install sphinx

          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi

      - name: Build PDF documentation
        run: |
          make latexpdf

      - name: Upload PDF artifact
        uses: actions/upload-artifact@v7
        with:
          name: documentation_pdf
          path: _build/latex/UFSC-CAT-A1-CDR-0001-*.pdf
